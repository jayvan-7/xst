token:abc
server:
  port: 9002
spring:
  application:
    name: user-consumer
eureka:
  client:
    service-url:
      defaultZone: http://root:ok@localhost:7001/eureka/
feign:
  hystrix:
    enabled: true
